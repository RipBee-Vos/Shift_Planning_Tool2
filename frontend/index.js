import React from "react";
import ReactDOM from "react-dom";
import ShiftPlanner from "./App";

ReactDOM.render(
  <React.StrictMode>
    <ShiftPlanner />
  </React.StrictMode>,
  document.getElementById("root")
);
